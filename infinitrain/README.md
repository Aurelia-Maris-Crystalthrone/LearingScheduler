# InfiniTrain 模拟训练框架（学习率调度器测例专用）

## 环境要求
- Python 3.8+
- PyTorch (>=1.10) 支持 CUDA
- transformers
- numpy

安装依赖：
```bash
pip install torch transformers numpy -i https://pypi.tuna.tsinghua.edu.cn/simple